# 🛠️ Module 2 : Manipulation (CRUD) & Conception

Savoir lire c'est bien, savoir écrire c'est mieux. Dans ce module, nous apprenons à modifier les données et la structure de la base.

## 🎯 Objectifs
- Concevoir une table robuste avec des types de données et des contraintes.
- Maîtriser le **CRUD** : **C**reate, **R**ead (vu au Mod 1), **U**pdate, **D**elete.

## 🏗️ DDL (Data Definition Language)
C'est le langage pour définir la STRUCTURE.
- `CREATE TABLE` : Créer.
- `ALTER TABLE` : Modifier (ajouter une colonne par ex).
- `DROP TABLE` : Supprimer (Radical !).

**Les Types de Données courants :**
- `INTEGER` : Nombres entiers.
- `VARCHAR(n)` ou `TEXT` : Texte (n = limite caractères).
- `DECIMAL(p, s)` : Nombres à virgule (prix).
- `BOOLEAN` : Vrai/Faux.
- `DATE` / `TIMESTAMP` : Le temps.

## ✏️ DML (Data Manipulation Language)
C'est le langage pour manipuler les DONNÉES.
- `INSERT` : Ajouter.
- `UPDATE` : Modifier.
- `DELETE` : Supprimer.
